<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MOMO" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">#FFFFFF</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.MOMO" parent="Base.Theme.MOMO" />

    <!--Login bottom sheet Fragment 스타일 -->
    <style name="AppBottomSheetDialogBorder20WhiteTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalBorder20WhiteStyle</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="AppModalBorder20WhiteStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/dialog_login_background</item>
    </style>

    <!--탭 스타일(사용X) -->
    <style name="BulletDotCardView" parent="CardView">
        <item name="android:elevation">0dp</item>
        <item name="cardElevation">0dp</item>
        <item name="android:backgroundTint">@color/blue</item>
        <item name="android:radius">4dp</item>
    </style>

    <!--팝업 메뉴 스타일 -->
    <style name="CustomPopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!--데이트 피커 스타일 -->
    <style name="CustomDatePicker" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="colorAccent">@color/blue </item>
        <item name="colorPrimary">@color/blue</item>
        <item name="android:textColorPrimary">@color/blue</item>
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <!--바텀 네비게이션 스타일 -->
    <style name="BottomNavigationViewStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/black</item>
        <item name="itemTextColor">@color/black</item>
        <item name="background">@color/white</item>
    </style>

    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemTextAppearanceActive">@style/CustomBottomNavigationActiveText</item>
        <item name="itemTextAppearanceInactive">@style/CustomBottomNavigationInactiveText</item>
    </style>

    <!-- 활성 상태 텍스트 -->
    <style name="CustomBottomNavigationActiveText" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">12sp</item>
    </style>

    <!-- 비활성 상태 텍스트 크기 -->
    <style name="CustomBottomNavigationInactiveText" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">12sp</item>
    </style>

    <!--스플래쉬 커스텀 -->
    <style name="App.Theme.SplashScreen" parent="Base.Theme.MOMO"/>

    <style name ="Theme.App.SplashScreen" parent = "Theme.SplashScreen">
        <item name = "windowSplashScreenBackground">@color/blue</item>
        <item name = "windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name = "postSplashScreenTheme">@style/Base.Theme.MOMO</item>
    </style>

</resources>